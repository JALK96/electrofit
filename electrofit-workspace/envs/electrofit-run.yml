name: electrofit-run
channels:
  - conda-forge
dependencies:
  - python=3.12
  # Core chem/MD tools needed by electrofit
  - ambertools>=23,<25
  - openbabel>=3.1
  - openmol
  - mdtraj
  - numpy
  - pandas
  - matplotlib
  - acpype
  # Optional for later steps:
  - gromacs

  - pip
  - pip:
      # If published: "electrofit" (PyPI)
      # For now, install your local package:
      - -e /home/johannal96/PhD.nobackup/electrofit/electrofit-workspace/packages/electrofit